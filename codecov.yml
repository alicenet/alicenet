ignore:
  - "bridge/bindings/**"
  - "cmd/**"
  - "proto/**"
  - "**/*.mockgen.go"
coverage:
  status:
    project:
      default:
        target: auto
        threshold: 2%
    patch:
      default:
        target: auto
        threshold: 2%
