run:
  timeout: 5m
  build-tags:
    - integration
    - flakes
linters:
  enable:
    - errcheck
    - varcheck
  # Disabling linters until we get them fixed up.
  disable:
    - gosimple
    - govet
    - ineffassign
    - staticcheck
