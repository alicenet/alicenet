run:
  timeout: 5m
  build-tags:
    - integration
    - flakes
linters:
  enable:
    - errcheck
    - govet
    - gosimple
  # Disabling linters until we get them fixed up.
  disable:
    - ineffassign
    - staticcheck
