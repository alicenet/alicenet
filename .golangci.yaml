run:
  timeout: 5m
  build-tags:
    - integration
    - flakes
linters:
  enable:
    - errcheck
    - gosimple
    - govet
    - ineffassign
    - varcheck
  # Disabling linters until we get them fixed up.
  disable:
    - staticcheck
